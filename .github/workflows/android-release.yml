name: Build Release APK

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-android:
    uses: ./.github/workflows/android-build.yml
    with:
      build_type: release
    secrets: inherit
